# push-helper
A web-based tool to push local folders to GitHub repositories directly from the browser, without needing Git CLI. Supports drag &amp; drop or folder picker, preserves directory structure, and auto-excludes sensitive files like .env and node_modules.
